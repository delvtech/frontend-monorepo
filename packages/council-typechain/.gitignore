.DS_Store
node_modules
cache
artifacts

# don't check in the compiled typescript factories
compiled

# Don't check in the compiled types, as they aren't the complete picture (no local contracts)
types

# we bundle in the dist/ so it's easy to use this repo as a npm package
!dist/types/

council