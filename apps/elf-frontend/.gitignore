# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# Running the tsc task in VSCode
tsconfig.tsbuildinfo

# production
/build

# these are copied in from the top-level scripts and will be different on every
# machine
src/canperform/*
# This definition file is an exception as it does not change on every machine
# and CI will fail without it.
!src/canperform/CanPerformJsonFile.d.ts
# This is a stub used for unit tests, it does not change on every machine
!src/canperform/mock.canperform.json
!src/canperform/goerli.canperform.json
!src/canperform/mainnet.canperform.json

# misc
.DS_Store

.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.yalc
yalc*

# These are generated fresh on every build
version.output.json
tailwind.output.css

.eslintcache

.next
